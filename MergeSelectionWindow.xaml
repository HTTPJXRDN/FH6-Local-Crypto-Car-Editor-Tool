<Window x:Class="FH6LocalCryptoTool.MergeSelectionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Choose donor changes" Width="900" Height="720" MinWidth="620" MinHeight="470"
        WindowStartupLocation="CenterOwner" Background="#FF1D1D1F" Foreground="#FFF4F4F6"
        Icon="/FH6LocalCryptoTool;component/Assets/ICO/dbtool_icon.ico" WindowStyle="None" ResizeMode="CanResize"
        TextOptions.TextFormattingMode="Ideal" UseLayoutRounding="True"
        StateChanged="Window_StateChanged">
    <Window.Resources>
        <SolidColorBrush x:Key="Ink" Color="#FFF4F4F6"/>
        <SolidColorBrush x:Key="Dim" Color="#FF9C9CA2"/>
        <SolidColorBrush x:Key="Line" Color="#FF3C3C40"/>
        <SolidColorBrush x:Key="Pink" Color="#FFE8175D"/>
        <LinearGradientBrush x:Key="AppGrad" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FF212123" Offset="0"/>
            <GradientStop Color="#FF191919" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="CardGrad" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FF303034" Offset="0"/>
            <GradientStop Color="#FF29292C" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="PinkGrad" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#FFFF2E70" Offset="0"/>
            <GradientStop Color="#FFE8175D" Offset="1"/>
        </LinearGradientBrush>
        <LinearGradientBrush x:Key="PinkAccentLine" StartPoint="0,0" EndPoint="1,0">
            <GradientStop Color="#FFE8175D" Offset="0"/>
            <GradientStop Color="#66E8175D" Offset="0.55"/>
            <GradientStop Color="#00E8175D" Offset="1"/>
        </LinearGradientBrush>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FF493039"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="#FF393036"/>
        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="#FFF4F4F6"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
        </Style>
        <Style x:Key="CaptionButton" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource Dim}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="34"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#FF34343A"/>
                                <Setter Property="Foreground" Value="{StaticResource Ink}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#FF2A2A2E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CaptionCloseButton" TargetType="Button" BasedOn="{StaticResource CaptionButton}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource Pink}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#FFC9124E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="GhostButton" TargetType="Button">
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="Background" Value="#FF313135"/>
            <Setter Property="BorderBrush" Value="{StaticResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Pink}"/>
                                <Setter TargetName="b" Property="Background" Value="#FF38383D"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource Line}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PinkButton" TargetType="Button" BasedOn="{StaticResource GhostButton}">
            <Setter Property="Background" Value="{StaticResource PinkGrad}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="b" Background="{TemplateBinding Background}" CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#FFFF4A84"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="b" Property="Background" Value="#FFC9124E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="b" Property="Background" Value="{StaticResource Line}"/>
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="Input" TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="CaretBrush" Value="{StaticResource Pink}"/>
            <Setter Property="Background" Value="#FF202023"/>
            <Setter Property="BorderBrush" Value="{StaticResource Line}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="11,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="b" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="b" Property="BorderBrush" Value="{StaticResource Pink}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Horizontal" Background="Transparent">
                            <Border x:Name="box" Width="20" Height="20" CornerRadius="5"
                                    Background="Transparent" BorderBrush="{StaticResource Line}"
                                    BorderThickness="2" VerticalAlignment="Center">
                                <Viewbox Width="14" Height="14">
                                    <Grid>
                                        <Path x:Name="tick" Data="M4 8 L7 11 L12 4" Stroke="White"
                                              StrokeThickness="2" StrokeStartLineCap="Round"
                                              StrokeEndLineCap="Round" StrokeLineJoin="Round" Visibility="Collapsed"/>
                                        <Path x:Name="partial" Data="M3 8 L13 8" Stroke="White"
                                              StrokeThickness="2" StrokeStartLineCap="Round" Visibility="Collapsed"/>
                                    </Grid>
                                </Viewbox>
                            </Border>
                            <ContentPresenter Margin="9,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="box" Property="Background" Value="{StaticResource Pink}"/>
                                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource Pink}"/>
                                <Setter TargetName="tick" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="{x:Null}">
                                <Setter TargetName="box" Property="Background" Value="#FF80334C"/>
                                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource Pink}"/>
                                <Setter TargetName="partial" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="box" Property="BorderBrush" Value="{StaticResource Pink}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="{StaticResource Ink}"/>
            <Setter Property="Padding" Value="3,2"/>
        </Style>
    </Window.Resources>

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="34" ResizeBorderThickness="6" CornerRadius="0"
                            GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border x:Name="RootBorder" Background="{StaticResource AppGrad}"
            BorderBrush="{StaticResource Line}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="34"/><RowDefinition Height="2"/><RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="#FF161618">
                <Grid>
                    <TextBlock Text="FH6 LOCAL CRYPTO TOOL  /  MOD MERGE" Foreground="{StaticResource Dim}"
                               FontSize="11" FontWeight="SemiBold" VerticalAlignment="Center" Margin="16,0,0,0"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource CaptionButton}" Content="&#xE921;" ToolTip="Minimize"
                                Click="MinimizeButton_Click"/>
                        <Button x:Name="MaximizeButton" Style="{StaticResource CaptionButton}"
                                Content="&#xE922;" ToolTip="Maximize" Click="MaximizeButton_Click"/>
                        <Button Style="{StaticResource CaptionCloseButton}" Content="&#xE8BB;"
                                ToolTip="Close" Click="Cancel_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
            <Border Grid.Row="1" Background="{StaticResource PinkAccentLine}"/>

            <Grid Grid.Row="2" Margin="24,20,24,20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/><RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <StackPanel Grid.Row="0">
                    <TextBlock Text="Choose changes to import" FontSize="21" FontWeight="SemiBold"/>
                    <TextBlock Text="Your database stays unchanged. Selected donor changes go into a new merged database."
                               Foreground="{StaticResource Dim}" Margin="0,7,0,0" TextWrapping="Wrap"/>
                    <TextBlock x:Name="SourceText" Foreground="{StaticResource Dim}" Margin="0,5,0,0"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>
                <Border Grid.Row="1" Background="{StaticResource CardGrad}" BorderBrush="{StaticResource Line}"
                        BorderThickness="1" CornerRadius="10" Padding="13" Margin="0,16,0,14">
                    <TextBlock TextWrapping="Wrap" Foreground="#FFD0D0D4" FontSize="12">
                        Expand a table to pick individual rows. "New" inserts a row; "Replace" overwrites only that matching row.
                        "Replace whole table" drops the staged table, recreates the donor schema, then copies the complete donor table.
                        Use exact replacement only when the donor intentionally defines the whole table. Related parts may live in other tables.
                    </TextBlock>
                </Border>
                <Grid Grid.Row="2" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Filter tables:" VerticalAlignment="Center" Margin="0,0,9,0"/>
                    <TextBox x:Name="TableFilter" Grid.Column="1" Height="34" Style="{StaticResource Input}"
                             TextChanged="TableFilter_TextChanged" ToolTip="Filter table names"/>
                    <Button Grid.Column="2" Content="Select new rows" Style="{StaticResource GhostButton}"
                            Margin="10,0,7,0" Click="SelectNew_Click"/>
                    <Button Grid.Column="3" Content="Clear" Style="{StaticResource GhostButton}" Click="Clear_Click"/>
                </Grid>
                <Border Grid.Row="3" Background="#FF232326" BorderBrush="{StaticResource Line}"
                        BorderThickness="1" CornerRadius="10">
                    <TreeView x:Name="TableTree" Background="Transparent" BorderThickness="0" Padding="10"
                              Foreground="{StaticResource Ink}" VirtualizingStackPanel.IsVirtualizing="True"/>
                </Border>
                <Grid Grid.Row="4" Margin="0,14,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock x:Name="SelectionText" Foreground="{StaticResource Pink}" FontWeight="SemiBold"/>
                        <TextBlock x:Name="WarningText" Foreground="{StaticResource Dim}" FontSize="11"
                                   TextWrapping="Wrap" MaxHeight="42"/>
                    </StackPanel>
                    <Button Grid.Column="1" Content="Cancel" Style="{StaticResource GhostButton}"
                            Margin="8,0" Click="Cancel_Click"/>
                    <Button x:Name="ApplyButton" Grid.Column="2" Content="Merge selected"
                            Style="{StaticResource PinkButton}" Click="Apply_Click"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
